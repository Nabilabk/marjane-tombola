# Never bake secrets or local-only state into the image — env vars are
# injected at run time instead (docker-compose.yml's `env_file: .env`).
.env
.env.*
!.env.example

__pycache__/
*.pyc
.venv/
venv/
tests/
last_scan_debug.txt
.git/
